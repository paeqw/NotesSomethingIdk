<Window x:Class="NotesSomethingAAAA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotesSomethingAAAA"
        mc:Ignorable="d"
        Title="NotesApp" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="TitleTextBox" Grid.Row="0" Margin="5" />
        <TextBox x:Name="NoteTextBox" Grid.Row="1" Margin="5" />
        <Button x:Name="SaveButton" Grid.Row="2" Margin="5,35,5,5" Click="SaveButton_Click">Save</Button>
        <ListBox x:Name="NotesListBox" Grid.Row="3" Margin="5" SelectionChanged="NotesListBox_SelectionChanged"/>
    </Grid>
</Window>